// Isolated visual verification. No production database, storage, mail, or jobs.
import {build} from 'esbuild';
import {randomBytes} from 'node:crypto';
import path from 'node:path';
import {pathToFileURL} from 'node:url';
const root=process.cwd();
for(const key of Object.keys(process.env))if(/DATABASE_URL|RESEND|EMAIL_FROM|SLACK|TWILIO|SENDGRID|SUPABASE|R2_|BOOTSTRAP|ANTHROPIC|CLAMD|SCHEDULER|MFA_|FILE_SCAN/.test(key))delete process.env[key];
Object.assign(process.env,{NODE_ENV:'test',APP_DATA_MODE:'demo',APP_TIMEZONE:'Asia/Qatar',APP_URL:'http://127.0.0.1:5192',JWT_SECRET:randomBytes(40).toString('hex'),JWT_REFRESH_SECRET:randomBytes(40).toString('hex'),MFA_ENCRYPTION_KEY:randomBytes(32).toString('hex'),MFA_ENFORCE_PRIVILEGED:'false'});
await build({stdin:{contents:`import express from 'express';import {previewSettlement} from './scripts/preview-settlement';await previewSettlement();import {registerRoutes} from './server/routes';import {securityHeaders,requestError} from './server/middleware/security';const app=express();app.use(securityHeaders());app.use(express.json({limit:'2mb'}));const server=await registerRoutes(app);app.use(express.static('dist/public'));app.get('*',(_req,res)=>res.sendFile(process.cwd()+'/dist/public/index.html'));app.use(requestError);server.listen(5192,'127.0.0.1',()=>console.log('Synthetic preview http://127.0.0.1:5192 — preview_admin / LocalPreview2026!'));`,resolveDir:root,loader:'ts'},outfile:'dist/remediation-preview.mjs',bundle:true,platform:'node',format:'esm',packages:'external',plugins:[{name:'isolated-db',setup(b){b.onResolve({filter:/[\\/]db$/},a=>path.resolve(a.resolveDir,a.path)===path.join(root,'server','db')?{path:'isolated',namespace:'preview'}:null);b.onLoad({filter:/.*/,namespace:'preview'},()=>({contents:`import {PGlite} from '@electric-sql/pglite';import {drizzle} from 'drizzle-orm/pglite';import {readFileSync,readdirSync} from 'node:fs';import bcrypt from 'bcryptjs';const pg=new PGlite();for(const f of readdirSync('migrations').filter(f=>f.endsWith('.sql')).sort())await pg.exec(readFileSync('migrations/'+f,'utf8'));await pg.query("INSERT INTO users(username,email,password,first_name,last_name,role,is_active,approval_status) VALUES($1,$2,$3,$4,$5,'super_admin',true,'approved')",['preview_admin','preview@example.test',await bcrypt.hash('LocalPreview2026!',4),'Synthetic','Administrator']);await pg.exec("INSERT INTO employees(employee_id,first_name,last_name,gender,date_of_birth,nationality,qid_number,primary_mobile,residential_address,emergency_contact_name,emergency_contact_number,department,position,location,type,joining_date) VALUES('PREVIEW-ONLY','Synthetic','Employee','female','1990-01-01','Test','PREVIEW-ONLY','00000000','Synthetic_address','Synthetic contact','00000000','Operations','Preview role','Test','permanent','2020-01-01')");export const db=drizzle(pg);export const pool={query:async q=>pg.query(typeof q==='string'?q:q.text),on:()=>{}};`,loader:'js',resolveDir:root}));}}]});
await import(pathToFileURL(path.join(root,'dist/remediation-preview.mjs')).href);
