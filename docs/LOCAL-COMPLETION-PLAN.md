# Local completion work

User direction, 15 September 2026: stop pushing and deploying; finish the remaining modules locally first. Do not push GitHub or deploy Render again without a subsequent user instruction.

Latest direction: "proceed ana later push it" authorizes a later consolidated push after the work and verification, not repeated pushes during implementation. Deployment remains paused.

Use MODULE-COMPLETION-CHECKLIST.md as the acceptance inventory. Keep implementation and verification status separate from the deployed release. Preserve the $0 budget. Do not populate production with synthetic records. Business calculations must be configurable by Admin/Super Admin rather than invented as fixed defaults. EOS, Resend sender setup and WhatsApp remain external integration dependencies.

## Implemented locally, not deployed

- Leave calendar now reads approved, scoped leave and omits private reasons. Random balance values are removed. Approval controls follow server capabilities; employees can cancel their pending requests. Successful changes refresh all leave views. Overlapping approvals are rejected.
- Migration 0015 adds an allocation ledger. Admin/Super Admin can enter signed whole-day allocations with a reason and idempotency reference. Accounts activate explicitly per employee/type/year. Available days derive from allocations less approved and pending requests; rejection/cancellation releases reservations. Atomic employee locks serialize allocations and requests. Cross-year requests split their saved calendar snapshot; legacy cross-year records without snapshots block managed-account activation until reconciled. Existing legacy balances are not silently imported. Type names remain stable accounting keys. Automated accrual/carryover, fractional days and legacy reconciliation still require implementation.
- Mobile leave uses the same validated calendar and reservation path. Employee dashboard uses managed ledger availability when present, otherwise its legacy balance source.
- Migration 0016 adds helpdesk knowledge articles and version history. Admin/Super Admin/HR Director can draft, publish, revise and withdraw publication. Employees see published content only. Search is literal, paginated and authenticated. Updates reject stale versions and roll back if history cannot be saved. General guidance is kept separate from private case messages.
- Migration 0017 adds versioned category response/resolution targets editable by Admin/Super Admin. New cases pin the policy and deadlines; later policy changes do not rewrite them. Targets count elapsed hours including weekends and employee waiting time. Only a public staff response satisfies the response target. Reopening retains original deadlines. The overdue queue retains confidential-case access restrictions. Automatic reminders, escalation and category auto-assignment remain pending.

## Verification

### Employee correction requests

Migration 0025 adds one pending contact/address/emergency correction per employee with original values, proposed values, reason and decision history. Employees can request changes only for their linked record and withdraw pending requests. Independent HR employee writers can approve/reject within employee scope. Finance/team readers cannot access private proposals. Approval checks the original employee record version; conflicts require a fresh request rather than overwriting intervening edits. Approval, employee update, decision and audit commit together, with field names only in general audit logs. The profile has request/review controls and the employee directory has an HR pending inbox. Employment, identity, account, salary and banking fields are excluded. Team-wide unified approvals, browser acceptance and expanded correction policies remain pending. 37 employee-record/migration tests, TypeScript and the server build passed; no push/deployment.

### Final-settlement controls

Migration 0024 adds itemized final-settlement drafts with earning/deduction amounts and mandatory calculation/supporting-record basis. Amounts are summed in integer cents; negative net totals are rejected. Duplicate employee/exit-date drafts are rejected. Draft edit, submission, independent approval/return, external payment reference and immutable revision snapshots commit atomically with audit records. Preparers (including earlier editors) cannot review their own work; employees cannot manage their own settlement. Paid records cannot be edited. The Payroll screen supports these actions and history within existing detailed payroll roles and row permissions. These are manually verified amounts: gratuity/leave-payout formula configuration, payroll overlap reconciliation, automatic payout generation and bank transfers are not implemented. Twelve focused calculation/migration tests passed before the final capability projection adjustment; the final 11-test calculation rerun, TypeScript and frontend/server builds also passed. No push/deployment occurred.

Latest consolidated local regression: 156 tests passed across 14 files after payroll reconciliation. TypeScript and frontend/server builds passed; the existing frontend bundle-size warning remains. This supersedes earlier focused-run counts for the current local code. The final Payroll-page integration was included in the frontend build. Browser acceptance for recently added workflows and the remaining implementation checklist are still outstanding.

### Payroll reconciliation

Added a read-only, employee-scoped monthly reconciliation endpoint and screen with CSV export. It recalculates against the record's saved policy, compares net and rounding adjustments, flags duplicate records and missing processed-payment evidence, and reports pending/processed totals using integer cents. Missing snapshots and invalid data are flagged and excluded from recalculated totals; partial totals are labeled. No current-policy substitution or bank receipt verification is implied. Nine calculation-rule tests and TypeScript passed; server build passed. Final-settlement calculations, reconciliation sign-off, payroll adjustment approval and approved-timesheet-to-pay mapping remain pending. No push or deployment yet.

### Offboarding and asset return

Migration 0023 adds one open exit case per employee, versioned task updates, checklist/asset kinds, owners, dates and completion evidence. The Onboarding screen now includes offboarding creation, task tracking and completion/cancellation. Every task must be done before completion. HR management handles cases; self-offboarding is denied. Optional account deactivation requires Admin/Super Admin, protects Super Admin accounts from ordinary administrators, and revokes sessions in the same transaction as case completion and audit. Task titles/types remain fixed after creation; cancellation permits a replacement case. Employment termination remains the existing employee lifecycle action. This does not calculate final settlement, manage an asset inventory, schedule future deactivation or provide reusable exit templates. Ten focused onboarding/migration tests and TypeScript passed; server build passed. Browser acceptance remains pending. No production data, push or deployment was touched.

### Accepted-offer handoff

Migration 0022 adds unique offer/application/candidate/employee links. HR employee writers can link an accepted offer with a recorded acceptance date to an existing active employee after exact trimmed QID matching and offer/application/employee conflict checks. The employee must be created and verified through the existing employee form; missing personal fields are never invented. Application status becomes hired in the same transaction as the link and audit entry. Identical retries return the existing link; conflicting links are rejected. Linked offers/applications are protected from later updates at the database layer. The Offers screen provides employee search, link confirmation and a link to onboarding checklist selection. This is a verified-record linking workflow, not automatic candidate-to-employee record creation or automatic onboarding start. Eight focused onboarding/migration tests passed. Browser acceptance and offboarding remain pending; no push/deployment occurred.

### Versioned onboarding checklists

Migration 0021 adds template versions, active template-task selection and preserved revision snapshots. Checklist create/edit now saves the template and all tasks in one transaction, with mandatory reason, strict validation and stale-version rejection. The editor loads existing tasks, uses PUT for edits, shows revision history and rejects unsupported employee types. Prior task rows are retained for existing employee assignments; new onboarding selects only the current active tasks. The legacy task-by-task POST now directs clients to the atomic checklist editor. Existing legacy templates have their prior contents preserved at the first edit. Six onboarding/migration tests passed, including rollback, access denial, old assignment preservation and new assignment dates; TypeScript and server build passed. Candidate-to-employee handoff, offboarding and browser acceptance remain pending.

### Onboarding workflow follow-up

Migration 0020 adds task versions. Starting onboarding now validates the employee/template, rejects empty or mismatched templates and duplicate active onboarding, computes civil due dates in UTC, and creates the checklist, tasks and audit entry in one transaction. Task updates validate status, active owner and due date, reject stale versions, serialize progress calculation per onboarding, and commit task/progress/audit changes together. Reopening clears completion dates. The Tasks screen now has an editor for status, owner, due date and notes. Generated employee/assignee names and email addresses have been replaced with real records (missing work email remains null). Management access remains organization-wide and permission-gated; this does not grant employee self-service task access. Three integration tests plus the migration test passed; TypeScript and server build passed. Browser acceptance, candidate handoff, template editing/version snapshots, onboarding record editing, document collection, offboarding and account deactivation remain incomplete.

### Manual escalation follow-up

Migration 0019 adds an active escalation flag. Requesters and authorized case handlers can flag unresolved requests with a public reason. Only a non-requester triage reviewer can clear an active escalation; resolving/closing the case clears it with a history entry. Reopening does not silently reinstate a cleared escalation. UI controls use server capabilities; the escalated queue retains existing case scope. Every escalation/clear action checks the case version and commits history atomically. No email is sent and no handler/access change is made. Automated deadline escalation and notifications remain pending. The focused helpdesk/migration suite passed 21 tests, including stale retries, unauthorized clearance, confidential queue filtering, resolution/reopening, and audit rollback. TypeScript and the server build passed.

### Category routing follow-up

Migration 0018 adds immutable category/confidentiality routing policies. Admins configure ordinary routing; Super Admins can also configure confidential routing. The editor supports eligible-handler search, disabling automatic assignment, reasons and revision history. A new case records its routing decision in internal history and checks the handler's current active/approved role before assigning. Self-requests and unavailable handlers remain unassigned for triage. Confidential routing never falls back to the ordinary category policy. Existing cases retain their assignment. Routing API, confidentiality, stale version, fallback and migration checks passed in a 19-test focused run; TypeScript and both frontend/server builds passed (the existing bundle-size warning remains). Escalation and notifications remain pending.

140 tests passed across 13 files before target-policy additions. The final mobile/dashboard changes also passed a focused 48-test rerun. After target-policy additions, 17 helpdesk/migration tests passed; this is not a claim that the complete expanded suite was rerun. Strict TypeScript and frontend/server builds passed after those additions. Isolated browser checks verified calendar rendering, employee search, a ledger adjustment from 20 to 22 days, knowledge draft creation, and saving a category policy at version 1 with two-hour response/eight-hour resolution targets. The frontend bundle-size warning remains. Synthetic preview data is separate from production.

Remaining checklist items are not considered finished until implemented and tested. Next local work: automated helpdesk escalation/notifications, lifecycle/recruitment checklists, payroll reconciliation, workforce revisions and the remaining module acceptance cases. Leave accrual/carryover and reconciliation are still pending. No deployment or push is authorized by this checkpoint.

## Accepted larger-batch roadmap

The user approved connected batches with consolidated testing and a later consolidated push. These are workstreams to finish, not claims that 20 new standalone applications are complete.

| Batch | Workstream | Current status |
| --- | --- | --- |
| 1 | 1. Team approval inbox | Local time/leave/performance lanes with independent permissions |
| 1 | 2. Team-lead dashboard actions | Local scoped inbox and staffing drill-down links |
| 1 | 3. Absence coverage and replacements | Local alerts, candidate checks, replacement offers and history |
| 1 | 4. Employee availability | Local declarations/cancellation integrated with eligibility |
| 1 | 5. Recurring rosters and shift revisions | Local bounded recurrence, retries and preserved revisions |
| 2 | 6. Attendance correction workflow | Local self-service requests, independent decisions and version history |
| 2 | 7. Roster-aware attendance reconciliation | Local completed-assignment / clock / timesheet comparison |
| 2 | 8. Leave accrual and carryover policies | Local versioned policies and administrator-posted runs |
| 2 | 9. Leave ledger legacy/fractional reconciliation | Local hundredth-day allocations, half-day requests and legacy year splits |
| 2 | 10. Time-to-pay, rate rules and adjustments | Local rate-based import, adjustments, reservation, release and paid locking |
| 3 | 11. Recruitment workflow completion | Local versioned hiring stages and decision history; further offer/requisition controls remain |
| 3 | 12. Onboarding document/task completion | Local document requirements, verification and completion gate |
| 3 | 13. Offboarding and settlement completion | Local exit history and scoped payroll/settlement follow-up; automatic calculations remain |
| 3 | 14. Performance cycles, goals and development | Local configurable cycles, independent publication, acknowledgement and goal progress history |
| 3 | 15. Training and certification lifecycle | Local course catalogue, assignments, evidence, independent completion and certificate dates |
| 4 | 16. Expense submission and approval | Local policy caps, self-submission, independent decisions and external payment references |
| 4 | 17. Benefits administration | Local versioned eligibility/caps, enrollment review and end-of-benefit decisions |
| 4 | 18. Helpdesk automation and notifications | Local in-app deadline reminders and optional escalation; external delivery remains pending |
| 4 | 19. Reports and operational analytics | Local scoped service/training/joiner reports, reconciled status totals and CSV |
| 4 | 20. EOS API and webhook contract | Local team credentials, assignment snapshots/change feed and idempotent activation links; outbound webhooks and EOS acceptance pending |

### Batch 1 implementation scope

Migration 0026 adds availability declarations, recurring-roster request records, shift revision snapshots and replacement links. Scheduling grants still need to cover each entire shift. Availability belongs to the linked employee and cannot overwrite accepted commitments. Unavailability blocks offers and acceptance. Recurrence creates 2–12 occurrences atomically within 90 days, preserves site wall-clock time, rejects ambiguous/nonexistent clock-change times, and supports idempotent retries. These limits are technical request bounds, not pay/leave calculation rules.

A replacement immediately cancels the original assignment and creates an offer; the place remains unfilled until acceptance. Shift revision creates a successor, cancels old offers/acceptances, and preserves history. It does not auto-offer the successor. Past/current shifts cannot be revised. Coverage alerts expose only operational absence status, not private reasons. All changes and audit records commit together.

The team approval inbox includes scoped actionable timesheets, leave and missing assignment reviews. It uses existing independent review and leave permissions, with no new authority implied by scheduling access. Employee correction, expense and settlement decisions remain in their existing screens. Dashboard staffing excludes superseded shifts. Workforce includes employee availability, recurrence, candidate/replacement tools, coverage alerts and revision history.

Verification before final clock-change handling: TypeScript passed and 53 combined workforce/time/review/migration tests passed. Final consolidated regression and production builds are being recorded below when complete. Browser acceptance remains outstanding; all changes are local, uncommitted and not deployed.

Final Batch 1 verification: 169 tests passed across all 14 test files, including positive approval-lane transitions, clock-change recurrence and atomic rejection of ambiguous occurrences. Final TypeScript check and frontend/server production builds passed. The existing frontend bundle-size warning remains. The duplicate roster offer form was removed in favor of the eligibility-aware candidates/replacements panel; the final frontend build includes this cleanup. No browser acceptance was performed for Batch 1, no production data was changed, and nothing was committed, pushed or deployed. Next connected implementation batch is attendance, leave accrual/reconciliation and time-to-pay rules (workstreams 6–10).

### Batch 2 — attendance, leave and time-to-pay

Migration 0027 adds attendance versions and database-written history; private correction requests; dated operation policies; fractional leave ledger values and year splits; and reserved payroll time entries. The management office calendar stays Sunday–Thursday, 09:00–17:00 Asia/Qatar. No production policy values were changed.

- Attendance: linked employees propose completed-time corrections; independent approvers with existing attendance row scope approve/reject. Employees may withdraw. Approval rejects an intervening version, recalculates under the original policy and writes history/audit atomically. Manual self-editing is blocked even for legacy roles with self-update access. Open clock sessions continue across midnight for up to 24 hours. Longer unresolved sessions require HR reconciliation. Existing manual recording remains for other employees within write scope.
- Reconciliation: completed accepted workforce assignments are compared with overlapping clock records and reported timesheets. Missing or differing evidence is flagged; no hours or payroll amounts are invented. Clock totals spanning multiple assignments require review. UI/API windows are bounded to 90 days and 300 rows; users must narrow larger windows. This does not add geofencing, QR or biometric providers.
- Leave: Admin/Super Admin publishes effective-dated monthly accrual, annual accrual cap, service eligibility, calendar-day proration and carryover cap. Runs preview and post per employee/type/period with permanent references and pinned policy evidence. Posting rechecks the preview; duplicates return the existing run. Carryover transfers available days out of the previous year and requires all source-year requests decided. Runs are explicitly administrator-triggered, not a background scheduler. No expiry/forfeiture automation was added.
- Reconciliation and fractions: ledger allocations support hundredths of a day. Optional single-date half-day requests require an enabled duration policy and reserve 0.5 days. Half-day absence still blocks workforce shifts on that date; AM/PM allocation is not implemented. Admins explicitly verify an opening available balance; existing used and reserved days are included when initializing the ledger. Legacy cross-year requests require an audited year split preserving their original total, rather than reinterpreting them with current calendars. Legacy balances are evidence only and are never silently imported.
- Time-to-pay: Admin/Super Admin publishes disabled-by-default hourly rate, per-timesheet overtime threshold/multiplier and cent-rounding policies by employee policy group. Payroll editors preview all approved timesheets whose shift starts in that payroll month in the site's timezone. Each line records approved minutes and the effective rate version. Signed adjustments require reasons. Import adds a separately identified allowance and reserves its timesheets; pending imports may be released with retained history. Recording an external completed-payment reference locks the imported sheets and preserves their revisions atomically. Own-time import/release/payment is rejected. This is an earning allowance, not replacement of basic salary: payroll must verify that basic salary does not already include those hours. Employee-specific negotiated rates, daily/weekly overtime aggregation, salary proration, automatic absence deductions and bank submission remain pending.

Verification: the initial full run passed 181 of 183 tests; the two failures were legacy clock-in message compatibility and migration statement boundaries. After fixing those, the 31-test auth/new-batch rerun passed, including two added self-edit/proration/rollback cases; the final migration rerun also passed. The suite now contains 185 tests across 15 files. Final TypeScript and frontend/server builds passed before the final wording/layout-only cleanup; the final frontend build is recorded below. Browser verification with an isolated in-memory database confirmed employee correction submission, independent admin approval with original/proposed values and updated checkout, and publication of a 1.25-day monthly accrual policy. Payroll import, half-day requests and carryover were covered by API integration tests; their complete browser acceptance is still pending. The existing frontend bundle-size warning remains. All work is local and uncommitted. No push, deployment, production data change or paid service was used.

Next connected implementation batch: recruitment completion, onboarding documents/tasks, offboarding/settlement completion, performance cycles/goals/development, and training/certification lifecycle (workstreams 11–15).

Final Batch 2 cleanup: frontend build passed after the policy field-order and wording changes. Migration 0027 also seeds history snapshots for existing attendance records before enabling history triggers; the final migration apply/rerun check passed. The isolated browser tab and preview server were closed after verification.

### Push checkpoint and Batch 3

The user subsequently requested a push. Batches 1–2 and the accumulated earlier improvements were committed as `d5ab14548f03dee06463dc16821cea2ed7c0339a` and pushed to `github/enhance/admin-calculation-rules` in malikamaan12/HR-management. The remote SHA was verified. Main and Render were not changed. The latest request, “next modules”, continues development locally; the following Batch 3 changes are uncommitted and unpushed.

Migration 0028 and `/api/lifecycle` add these connected workflows:

- **Recruitment:** paginated hiring stages with current versions, decision reasons and transactional history. New applications start at New; arbitrary legacy status PUTs redirect callers to the versioned action. Screening, shortlisting, interview and offer stages advance in order. Offer stage requires a completed interview; rejection requires existing pending/accepted offers to be resolved first. Only accepted-offer handoff sets Hired. This is not completion of requisition approvals, interview scheduling conflicts or the entire legacy offer editor; those still need dedicated controls.
- **Onboarding:** HR adds required document types to active onboarding, then independently verifies an existing current uploaded document for that employee. Verification pins the file record's update timestamp. Final task completion rejects missing, expired, future-issued or replaced evidence. The check and task update roll back together; a database trigger also protects legacy parent writes. Requirements must be added before completion. Files remain in the existing private Documents workflow. Requirement templates, employee task ownership, notification delivery and requirement waiver/cancellation are not included. Completed onboarding is historical evidence rather than continuous compliance monitoring.
- **Exit follow-up:** immutable version snapshots for checklist changes, including an initial migration snapshot for old cases; acting user recorded for route updates. HR can review clearance status and recent settlements/pending payroll only within payroll permissions. The panel is a follow-up view, not an automatic payment or legal clearance decision. Final settlements retain their existing independent approval and manual verified amounts; gratuity formulas, bank execution and automatic payroll reconciliation sign-off remain pending.
- **Performance:** Admin/Super Admin creates cycles with immutable weighted criteria totalling 100%, a 5- or 10-point scale, and 0–2 decimal rounding. An eligible, independent assigned reviewer submits all criterion evidence and a development plan. Independent HR publishes or returns the review; employees see only published assessments and published history and may acknowledge receipt. Closed cycles cannot receive review changes; closure requires every assigned review published. Cycle assessments are separate from legacy ad-hoc reviews and legacy dashboard metrics. Goals created through the existing Goals tab now have versioned progress/status/date updates and evidence history; stale updates fail and reopening clears the completion date. Legacy goal PATCH directs callers to the audited endpoint. Formal calibration panels, structured plan milestones, reviewer reassignment and legacy review migration are not included. Ratings do not update pay or employment status.
- **Training:** a new Training and development navigation item opens a local course catalogue and scoped learning records. Admins add courses; authorized users assign an active course with a due date. The employee submits evidence, and an independent authorized reviewer returns it or records verified completion with a completion date and optional certificate expiry. Withdrawal and reassignment retain previous records; simultaneous active assignments for the same employee/course are prevented. Terminal records cannot be rewritten. History and audit commit atomically. These are internal records with no paid provider call; LMS synchronization, file upload within learning, course editing/archival, automatic certificate renewals and automatic workforce qualification grants remain separate work.

Validation: the consolidated suite passed 193 of 194 checks; the only failure was a new assertion comparing numeric score string `3.6` with `3.60`. The assertion now compares the numeric value. After the final goal and rating-policy changes, all 11 lifecycle integration tests plus migration apply/rerun passed (12 checks). The current suite contains 196 tests across 16 files; this is not a claim of a single final 196-test run. TypeScript and frontend/server production builds passed. The existing frontend bundle-size warning remains. Isolated browser acceptance verified course creation, scoped employee search, assignment, independent completion and certificate expiry display; other new UI paths have integration coverage but not complete browser acceptance. Preview tab and server were closed. No production data, deployment, push or paid service was used for Batch 3.

Next larger batch: expenses, benefits, helpdesk notifications, reports and the HR-side EOS API contract. The explicit limitations above remain part of the completion inventory.

### Batch 4 — employee services, reminders, reports and EOS

Migration 0029 adds service policies/requests, helpdesk reminder policies and records, EOS credentials, activation reference mapping, idempotency records and transactional assignment events. Batch 3 and this batch remain local, uncommitted and unpushed. No production settings or data were changed.

- **Expenses:** linked active employees submit their own QAR expense with service date, receipt/reference and purpose. Admin/Super Admin publishes versioned per-request and calendar-year caps and eligible employment types. The request pins its policy. Pending, approved and paid amounts reserve the annual cap; rejected/withdrawn requests release it. Another authorized payroll editor with all/department scope approves or rejects. Paid recording requires an already approved expense and an external completed-payment reference. This does not execute payment, upload/OCR receipts, reimburse through payroll or provide tax treatment. Self-approval and self-payment recording are denied, stale decisions conflict and audit failures roll back. Same-employee references are unique rather than silently creating duplicates. Expense self-service is available to linked employees; reviewer access stays within existing detailed payroll permissions and excludes team-only readers.
- **Benefits:** an employee with existing benefits enrollment rights requests an eligible policy, value and period within one calendar year. Policy caps reserve submitted/approved/ended values, and overlapping active enrollments for the same policy are rejected. Independent benefits editors decide requests or end an approved benefit. Ended records retain their recorded period/value and consumed allocation as history; ending does not calculate a refund. No dependants, insurer API, claims processing, automatic deductions, proration or cross-year enrollment is implemented. Values represent requested entitlements, not cash paid. New policy versions affect new submissions; existing requests retain their submitted policy.
- **Helpdesk:** administrators may enable in-app deadline reminders and optional automatic escalation. A worker checks every five minutes while the process is awake, with an administrator-triggered check for catch-up. It processes up to 100 cases per run using saved response/resolution deadlines; reminder uniqueness prevents repeated notifications. The requester and currently eligible assigned responder receive references only, not private titles or messages. Reading the reminder rechecks case access. The worker runs under the policy publisher's current active administrator role/scope; loss of that role stops it, and ordinary administrators do not acquire confidential access. Existing reminded deadlines are not repeatedly escalated after manual clearance. Free-host suspension means timing is best effort, not guaranteed. Resend/email/WhatsApp delivery remains unconfigured and no external notification was sent.
- **Reports:** Reports now includes joiners by joining date, service requests by service start date and training by due date, with current statuses, row scopes, date ranges of at most 366 days and up to 10,000 export rows. Financial totals are integer cents grouped separately by status. Team-only compensation access cannot export detailed financial data. The CSV uses the same query and neutralizes spreadsheet formula prefixes. The browser preview shows at most 100 rows and labels that limit. These reports do not certify historical headcount, turnover, bank reconciliation or payroll totals, and do not rewrite the legacy analytics definitions.
- **EOS:** Service operations allows administrators to create/revoke a one-team service credential with expiry within 365 days; the secret is returned once and only its hash persists. Active creator role and credential expiry/revocation are checked per request. The separate versioned service API supplies minimal assignment snapshots, cursor-based assignment changes and idempotent external activation references. It cannot mutate employees, pay or shifts. Change events commit with assignment changes; failed assignment transactions emit nothing. The polling contract is documented in `EOS-API-v1.md`; the older OpenAPI draft is still a broader proposal, not an implemented contract. Outbound signed webhooks, callback configuration, inbound scheduling, retention jobs and actual EOS acceptance remain pending.

Validation: **204 tests passed across all 17 files in one consolidated run**, including migration apply/rerun, request history/rollback, annual reservations, benefits overlap, exact report totals/CSV safety, reminder deduplication/privacy and EOS team scope/revocation/idempotency. TypeScript and frontend/server production builds passed. Subsequent changes only added page titles, isolated expense/benefit form state and retained the saved policy selection; the final frontend rebuild passed. The existing bundle-size warning remains. An isolated in-memory browser check verified administrator policy creation at 150.25 QAR per request / 500 QAR annually, a 75.25 QAR employee submission with no self-approval button, and a matching 75.25 QAR report total. Other flows have API coverage but not complete role-by-role browser acceptance. The temporary browser tab and local preview server were closed.

The 20-workstream roadmap now has local implementations in each batch, with the explicit remaining features above. This is not certification that every original or legacy module is complete. Next completion work should address the listed workflow gaps and acceptance before production deployment, alongside external sender and EOS setup when available.

### Batch 5: approval and administration controls (local)

Migration 0030 and `/api/workflow-controls` add five connected improvements:

- Requisitions now start as drafts. HR can edit the draft, submit, independently approve/return, open, hold, close or cancel. Requesters and recorded draft editors cannot approve their own work. Pending offers and accepted offers awaiting employee handoff prevent closing/cancellation. Version checks reject stale decisions.
- Offers now start as drafts with the creator derived from the authenticated account. Submission, independent approval, acceptance, decline, expiry and withdrawal have explicit transitions, recorded reasons and history. Acceptance validates the offer period and remaining vacancy capacity under a requisition lock. Draft and approval-pending offers also prevent application rejection. Replacing offer terms currently requires withdrawing and recreating the offer; pending legacy offers without approval metadata require this before acceptance. Previously accepted offers retain their existing handoff path.
- Admin/Super Admin publishes versioned document-type requirements per onboarding checklist. New onboarding cases copy the latest version; existing cases retain their original requirements. History includes all published versions. Waivers and requirement cancellation remain pending.
- Admin/Super Admin can update course titles, descriptions and providers, or archive/reactivate courses. Archived courses reject new assignments/enrollments; existing learning evidence and title snapshots remain intact.
- HR can reassign an unpublished assessment in an open review cycle to another eligible reviewer. The reassigned assessment starts a fresh draft; previous ratings remain in history and the old reviewer loses submission authority.

Requisition and offer history records controlled changes from this batch onward; migration does not reconstruct past decisions. Administrator settings do not bypass independent approval. No external email, paid service, production data change, push or deployment was performed. Interview scheduling conflicts, offer term editing, onboarding task ownership/waivers, settlement formulas and full role-by-role browser acceptance remain follow-up work.

Batch 5 verification: all 209 tests across 17 files passed in one consolidated run, including five new workflow cases and complete migration apply/rerun coverage. TypeScript and frontend/server production builds passed. The existing frontend bundle-size warning remains. This batch has API integration coverage but has not received a new browser acceptance run. All changes remain local, uncommitted and unpushed.

### Batch 6: hiring delivery and assigned onboarding (local)

Migration 0031 extends the previous controls with four connected workflows:

- Interview scheduling: bookings and rescheduling use explicit UTC-offset times, 15–480 minute durations, active interviewers and interview-stage applications against open requisitions. One scheduling transaction lock serializes overlap checks across both candidates and interviewers. Adjacent bookings are allowed. Completion/no-show decisions require the interview to have started; completion requires feedback, a 1–5 rating and recommendation. Completed/cancelled/no-show records are final. Every controlled change retains a version snapshot. Legacy interview creation/update endpoints now redirect callers to these controls. The UI uses Qatar time. Old appointments default to 60 minutes and retain their existing stored timestamp interpretation; no historical timezone conversion was attempted. Room reservations, external calendar invitations, panel interviewers, and conflicts with workforce shifts/leave are not included.
- Offer terms: only current drafts can change salary, dates and benefit terms. Edits clear prior approval and the old offer-letter reference; all recorded term editors are excluded from independent approval. Already approved/accepted offers cannot be silently rewritten. The legacy arbitrary offer PUT is blocked. Salary retains the existing whole-QAR schema. Generated letters/e-signature, negotiated fractional amounts and a return-to-draft action for already issued offers remain pending.
- Assigned onboarding: new checklist tasks marked `new_hire` automatically assign to that employee. Other task owners remain assigned by HR through the existing editor. Both staff and management dashboards show a paginated My onboarding tasks panel. Only the current active linked owner may update progress/evidence; owners cannot change assignments/due dates or reopen completed tasks. Completion retains the existing document gates and atomic parent progress update. Existing unassigned tasks are not silently reassigned. Task reassignment still uses HR's existing controls. Separate owner-submission/HR-acceptance states and notifications remain future work.
- Document waivers: Admin/Super Admin publishes an eligible-document-type list, empty/disabled by default. An independent administrator may waive an unverified requirement on active onboarding only when its type is allowed, with the policy version and reason recorded. Revocation restores Required. Waived records cannot be verified without revocation; completed onboarding freezes requirements. Both service and database completion guards recognize waivers. Changes to eligibility do not retroactively invalidate recorded waivers. Configuration is an organizational choice, not a legal compliance determination or automatic exemption.

Verification: all 214 tests across 17 files passed in one consolidated run. TypeScript and frontend/server builds passed. Isolated browser checks confirmed the booking form and interviewer choices, the assigned-task dashboard panel, and saving an education waiver policy as version 1. Full interview/offer/task interactions have API coverage rather than complete browser acceptance. A small empty-option rendering cleanup was applied after the browser check; final frontend build result follows. The existing frontend bundle-size warning remains. Preview server/tab were closed. No production records, external notifications, paid services, push or deployment were used; changes remain local and uncommitted.

Final Batch 6 follow-up: preview logs exposed a pre-existing onboarding-summary SQL error caused by an unqualified employee identifier inside a correlated subquery. Qualified the parent reference and made summary failures visible instead of showing zero counts. Added a regression for real employee names and summary access; all 10 onboarding tests passed after this fix (the suite now contains 215 tests). Final TypeScript, frontend and server builds passed, as did the diff whitespace check. The full 214-test run preceded this targeted correction; no second full-suite run was needed. The bundle-size warning remains.

### Batch 7: onboarding review and connected scheduling (local)

Migration 0032 adds administrator-configured onboarding review and completion guards. New-case tasks copy the current review policy for HR, Manager, IT and/or New Hire owner groups; the default policy requires no extra review. Existing cases keep their saved behavior. Owners submit evidence on review-required tasks; independent HR can approve or return it. Owner, submitter and onboarding subject cannot approve their own work. Pending submissions block direct edits; rejected/returned evidence and later resubmissions retain version history. HR can reopen completed tasks through the existing editor, requiring a new submission and decision. The database protects review-required task and parent completion. Uncontrolled legacy task creation now directs callers to checklist-based onboarding. Review and waiver policy history are available in the admin screens, and the review queue can include completed/cancelled cases for history access.

Interview bookings now check interviewer employment dates, approved leave, accepted workforce assignments, legacy attendance schedules and active event staff assignments. Dates use Qatar civil days for leave/employment and exact time intervals for schedules. Scheduling locks the interviewer employee record, sharing serialization with workforce/leave workflows. Supported reverse operations also reject overlapping interview slots: workforce eligibility/acceptance, legacy shift/event assignment helpers, and leave approval require cancellation or rescheduling first. Adjacent intervals remain allowed. This does not import external calendars, check room availability, or automatically cancel existing conflicts. Declared workforce unavailability and per-day management office-hour rules are not additional interview constraints in this batch.

Issued pending offers now have an explicit Revise action. It preserves the old terms, clears approval and the offer-letter reference, and returns the offer to draft. The revising actor joins the editor list and cannot approve the revised version. Accepted/handed-off offers remain final. The reason should record candidate communication; the application does not send that communication or automatically generate replacement documents.

Verification: the consolidated suite passed all 219 tests in 17 files, including policy pinning, return/resubmit/approve, administrator self-review denial, audit rollback, preserved offer revisions, and both scheduling directions. TypeScript and frontend/server builds passed. The isolated browser verified independent approval of a synthetic safety-orientation submission and the onboarding summary changing from in progress/0% to completed/100%. The review-history filter was added after that check; all 25 lifecycle tests passed in the targeted follow-up. Final build results follow. The preview tab/server were closed, and no live data, email, push, deployment or paid service was used. All work remains local and uncommitted.
Final Batch 7 TypeScript and frontend/server builds passed after the history filter and completed-case action visibility changes. The existing frontend bundle-size warning remains.

### Batch 8: equipment custody and policy acknowledgement modules (local)

Migration 0033 adds Equipment and assets (`/equipment`) and Policy acknowledgements (`/handbook`) with four connected workflows:

- Asset register: Admin/Super Admin records unique normalized asset tags, names, categories, serial numbers and storage locations; changes preserve version history and reject stale edits. Available, maintenance, retired and lost states are explicit. Assets under active custody cannot be edited. The register supports office equipment, event kits and FEC/mall activation equipment without changing employment types.
- Equipment custody: organization-wide HR employee managers issue available equipment to active employees with issue date, due date and condition evidence. Employee locks and a unique active-custody index prevent concurrent double issuance. Self-issuance is denied. Employees see only their linked custody, acknowledge receipt, and request return/report issues. Independent HR records return inspection or loss resolution and the resulting asset state; close/history/inventory changes commit atomically. Existing issue snapshots retain the original asset description. No depreciation, procurement, consumable stock, barcode scanning, accounting entries or payroll deductions are included.
- Offboarding connection: open custody counts appear with exit cases. Completing an exit requires all equipment custody resolved in addition to the existing checklist. A transaction check and database trigger enforce the guard; new issuance is blocked for employees with open/completed exit cases. Return processing remains available during exits. Rehire-specific clearance of historical completed exit cases remains a follow-up workflow; the current issuance block is conservative.
- Policy acknowledgements: Admin/Super Admin writes drafts, publishes, revises, withdraws outstanding requests and explicitly assigns the current publication to newly eligible employees. Audience filters are employment type and optional exact department, with a saved due date; publication is bounded to 10,000 eligible employees. Each assignment links to immutable text/version history. Only the linked employee can acknowledge receipt/reading; repeats retain the same timestamp. Publishing a revision replaces outstanding earlier requests, while prior acknowledgements preserve their original text. Preparing a new draft leaves old outstanding assignments active until publication/withdrawal. HR can review paginated recipients/statuses and version history. Plain text is rendered without HTML execution. This is acknowledgement recording, not electronic contract signature; reminders, external delivery, attachments and bulk exports are not included.

Verification: all 226 tests across 18 files passed in one consolidated run, including seven new custody/acknowledgement workflow tests, offboarding blocking, concurrent issuance, privacy, immutable revisions and audit rollback. TypeScript and frontend/server production builds passed. Browser checks in an isolated in-memory database verified asset creation (QA-KIT-01, Synthetic FEC tablet, available) and both new navigation/pages. Full receipt/return and publication/acknowledgement flows have API coverage rather than complete role-by-role browser acceptance. The existing frontend bundle-size warning remains. The preview server and tab were closed. No live records, email, paid services, push or deployment were touched; this batch remains local and uncommitted.


### Batch 9: probation, employee transfers and operational incidents (local)

Migration 0034 adds three connected modules under /probation, /transfers and /incidents, with authenticated /api/people-operations endpoints. All mutations validate inputs, reject stale record versions, and commit restricted record history and metadata-only activity logging in the same transaction. Admin/Super Admin publishes versioned probation extension limits and severity-specific incident targets; new records pin the policy and existing records retain it. Default extension limits are zero. Default incident targets are 168/72/24 elapsed hours from reporting for low/medium/high severity. These are configurable operational targets, not legal deadlines or automatic decisions.

- Probation: organization-wide HR opens an explicit review period with objectives and an independent reviewer (active HR manager or the employee's current reporting manager). One open/submitted case per employee is allowed. The assigned reviewer submits evidence and a recommendation; another HR manager confirms completion, extends within the saved limits, publishes a follow-up decision, or returns the review for more evidence. HR can cancel or reassign an active review, resetting current evidence while retaining its history. Self-review and self-decision are denied. Former reporting managers lose access. Employees see only their published confirmed/follow-up decision, and can record a response acknowledging receipt rather than agreement. Confirmation adds an employee lifecycle event. This does not change employment status, calculate statutory probation periods, terminate employment, generate letters, notify users or automatically derive an outcome from ratings. Internal earlier drafts/history remain restricted to HR and the currently eligible assigned reviewer.
- Transfers: HR proposes department, position, location and reporting manager changes with a saved professional-only before/after snapshot and employee version. Another HR manager approves or rejects. Approval does not update the profile; HR explicitly applies it on or after its effective date. Application rechecks active employment, employee version and reporting cycles through both primary and secondary managers, then updates the employee profile and lifecycle event atomically. Hierarchy writes share the existing employee table lock. Concurrent active proposals are prevented; changed profiles require cancellation and a replacement proposal. The employee sees approved/applied transfers only. Salary, employment type, schedules, team memberships and existing event assignments are outside this transfer workflow. Existing authorized employee-profile correction endpoints continue to exist; this batch does not force every direct profile edit through transfer approval.
- Incidents: authenticated reporters record an operational category, severity, location, Qatar-time occurrence, description and optional currently accessible workforce team (event/FEC/mall activation). Current membership or a scheduling grant is required to select a team unless the reporter is organization-wide HR. HR assigns a corrective action to an active HR handler or a current scheduling lead for that team. Only the assigned eligible handler resolves it; an HR manager other than the reporter and handler verifies closure. HR can reopen, clearing current assignment/resolution while preserving history and the original deadline. Reporter, currently eligible assigned handler and organization-wide HR can read the case and history; unrelated team readers cannot. Revoked scheduling grants remove handler access. All notes here are shared operational notes; confidential grievances and medical details belong in HR Helpdesk. No payroll deductions, employment decisions, attachment upload, external delivery, automated escalation or statutory incident filing is performed.

Validation: TypeScript and frontend/server production builds passed. The consolidated 236-test run passed 235 tests, including all 10 new workflow tests; its migration runner caught missing statement separators between the new indexes. After adding the separators, the deployment migration test passed against an empty database and on a safe rerun. No other code changed after those checks. The browser preview used an isolated in-memory database and synthetic accounts: a transfer proposal was saved with a visible before/after comparison, an incident was saved with the correct Qatar time and 72-hour target, and probation navigation/form loading passed. Full independent decisions, extensions, stale/cycle checks, grant revocation, privacy and audit rollback are API-tested rather than complete role-by-role browser acceptance. The existing frontend bundle warning remains (main minified bundle approximately 1.667 MB). No production data, settings, external messages, paid service, push or deployment was changed. This batch and the preceding local batches remain uncommitted.


### Batch 10: contract renewals, reusable exit checklists and return-to-work equipment clearance (local)

Migration 0035 adds /contracts, /exit-templates and /return-to-work with authenticated /api/employment-continuity endpoints and connections to existing offboarding and equipment custody. These remain local, uncommitted and unpushed.

- Contract renewals: organization-wide HR sees a paginated expired/expiring queue for active/on-leave employees with a contract end date and no termination. Admin/Super Admin edits the look-ahead window (default 30 days, versioned history). The current rule affects the read-only watch queue; it does not extend contracts or send reminders. HR proposes a strictly later end date, effective date, verified agreement reference and reason against the current employee version. One pending renewal is allowed. Another HR manager approves/rejects; HR applies an approved renewal on or after its effective date. Approval/application recheck employee state, unchanged profile, unexpired proposed end date and absence of an open exit case. Application updates only contract end date and adds a lifecycle event, with history/audit in the same transaction. Employees see only their own approved/applied renewals. Cancel-and-replace handles changed terms. Existing direct profile editing remains available to authorized HR; this is not electronic signature, contract generation, payroll revision, automatic activation or enforcement of a universal approval requirement on every profile correction.
- Exit checklist templates: Admin/Super Admin creates, edits or disables named versions, selects permanent/temporary/contract audiences, and defines up to 100 checklist/asset tasks with configurable day offsets from target exit date. HR selects an applicable enabled version, target date, explicit active non-departing task owners and case reason. Server-side expansion rejects missing/duplicate/forged owners and client task overrides; all tasks start pending, calculated civil dates are saved, and each case retains the complete source template/version. Changing a template does not rewrite existing cases. Existing manual exit checklists and HR-managed task completion still work. The offboarding screen links to templates and shows saved template/target date. New completion evidence and reasons stay in restricted case history; general activity logs now contain only action metadata. Previously stored logs are not rewritten. This does not schedule termination/account deactivation, calculate settlement, automate owner assignment or add a self-service offboarding owner task inbox.
- Return-to-work equipment clearance: HR must first verify and restore the employee's active employment record through existing employee management. This new workflow does not hire, reactivate employment, reset original service dates, change roles or enable login. HR records the verified return date, employment approval reference, reason and current employee version. The request captures the exact versions of completed exit cases that still block equipment. An independent HR manager approves/rejects after rechecking current employment, dates, no open exit or outstanding custody, unchanged employee version and unchanged exit versions. Approval allows new equipment issuance only on/after the return date and only past those reviewed completed exits. New open/completed exits, corrected exit versions or revocation block fresh issuance. Revocation does not erase existing custody. A version comparison avoids relying on transaction-start timestamps when concurrent exit completion waits are involved. Pending clearance may be cancelled; approved clearance may be revoked with a reason. This closes the earlier conservative completed-exit equipment block without deleting exit history or automatically activating accounts.

Verification: the consolidated suite passed all 246 tests across 20 files, including the 10 new employment-continuity scenarios and deployment migration safe-rerun coverage. TypeScript and both production builds passed; the existing frontend bundle-size warning remains (main minified bundle approximately 1.684 MB). Browser checks with an isolated in-memory database and synthetic users created an enabled exit template and an exit case using that version, with the selected owner and the expected two-days-before-exit task deadline; contract renewal and return-to-work screens also loaded. Independent renewal/clearance decisions, stale protection, revocation and audit rollback are API-tested rather than complete role-by-role browser acceptance. A final server refinement matched equipment clearance against exact exit versions instead of transaction timestamps; targeted regression and final server build results are recorded below. Preview tab and server were closed. No production records, external messages, paid services, pushes or deployments were touched.

Final Batch 10 follow-up: all 17 affected employment-continuity/equipment tests passed after the exit-version refinement, including transaction-time independence and changed-exit-version blocking. Final TypeScript and server build passed; the frontend was unchanged from its successful build and browser check. The diff whitespace check passed.


### Batch 11: document approval rules, reviewer ownership and compliance register (local)

Migration 0036 extends the existing private document workflow. Documents now has Register, Renewal requests and Approval rules tabs. This batch remains local, uncommitted and unpushed.

- Admin/Super Admin configures optional approval, required self-service replacement approval, or required approval for all replacements (including administrators), optionally limited to exact document types. The default remains optional for compatibility. Enforcement runs before upload and again under the document/policy locks, so a rule tightened during upload prevents replacement and cleans the unused object. Initial uploads retain existing permissions; this is not an approval gate for creating the first document record.
- Admin/Super Admin also sets a named-reviewer requirement and review target of 1–365 calendar days (default 7). Each new renewal stores the full policy version and Qatar due date; changing current rules does not rewrite pending requests. Legacy requests retain their original optional assignment behavior and have no invented deadline. Overdue is a work-queue flag, without automatic approval, rejection, notification or escalation.
- Scoped HR document editors can assign, reassign or clear a reviewer on a pending request with a reason and current request version. The searchable candidate picker includes active, approved, independent users with current document-update access to that employee. An assignment never expands read scope. When assigned, only that reviewer may approve/reject; requester and employee self-review remain forbidden. Required assignment blocks unassigned decisions. Current eligibility is checked again at decision time. Employees can withdraw their own submitted requests.
- Request creation, assignment and final decisions record restricted version history and metadata-only activity entries in the same transaction as the change. Stale assignments/decisions conflict; document version checks prevent approval overwriting a later replacement. Older clients may omit the request version only while it is still version 1, preserving first-decision compatibility without allowing decisions on reassigned requests. Proposal files remain private and existing file-version retention continues. History does not copy file keys or proposed document contents; submitted/decision reasons remain scoped to authorized request viewers.
- Register search, exact document-type/status filters, counts and 25-row pages run after employee access scope. Expiry is ordered by date and ID, recalculated from the current company expiry window and Qatar date; document detail, legacy lists and replacement status use the same civil-date rule. The old array endpoints remain available for existing consumers. Renewal queues filter submitted-by-me, assigned-to-me, unassigned and overdue requests, with paginated history. Uploads and decisions refresh document and dashboard caches.

Validation: the consolidated suite passed all 255 tests across 21 files, including 9 new governance tests and safe migration reruns. A final Qatar-midnight consistency fix added a tenth test; all 46 affected document/employee tests passed afterward. TypeScript and production builds passed. Isolated browser acceptance saved an all-replacements policy and named-reviewer target, assigned a synthetic renewal, approved it through the assigned reviewer, and verified that the register showed its new number and dates while direct replacement was hidden under the required-approval policy. Permission boundaries, races and audit rollback were API-tested. The main frontend bundle remains approximately 1.690 MB minified. No production records, paid services, messages, push or deployment were touched. Preview server and tab were closed.

Remaining document work: choose the actual organization approval policy, production private-storage acceptance, retention/cleanup rules, optional review notifications and broader role-by-role browser acceptance. This batch does not implement identity verification, electronic signatures or automatic legal-compliance decisions.


### Batch 12: reviewed bulk employee imports (local)

Migration 0037 replaces the immediate-write CSV import with saved drafts at /bulk-import. Only Admin/Super Admin can use the API or page, and each job, staged row, report and history remains private to its uploader. Work is local, uncommitted and unpushed.

- Upload and template: UTF-8 comma-separated CSV, up to 500 employee records and 2 MB. The header-only template and field guide cover permanent/temporary/contract employees, event eligibility, explicit work schedules, contract end dates, existing/same-file manager employee references, contact details and selected professional fields. Employee ID is now required; no random employee number is assigned. Phone/identity values stay strings, including leading zeros. Dates use strict YYYY-MM-DD and the same core employee-profile validation. Common snake-case/space aliases and email → personalEmail are accepted; duplicated aliases, unsupported columns, bad quoting, mismatched field counts and invalid encodings reject the whole upload. Credential, role and protected system columns are rejected before persistence. This flow creates employee records only; account creation and permissions belong in User Management.
- Draft review: uploading creates no employee or login account. Staged rows retain their field values in the database; source file bytes are not stored. Paginated review supports errors/included/excluded/imported filters, a field guide, per-record corrections and explicit exclusion with a reason. Every saved change revalidates the whole draft, including dependent managers and duplicate pairs, and advances the job version. Conflicting edits fail instead of overwriting a newer correction. CSV record numbering starts at 2 for logical data records (blank lines are skipped and quoted line breaks remain in their record).
- Validation and hierarchy: missing fields, invalid enum/boolean/integer/date/email data and inconsistent employment dates are reported by field. Trimmed, case-insensitive employee IDs and identity references are checked against included rows and current employees. Both reporting-manager paths reject missing/ambiguous or inactive managers, excluded/invalid dependencies, self-reference and cycles among new employees. Cross-file staff may reference an existing employee after that employee has been imported.
- Commit: the administrator reviews the included count/version and supplies a reason. The server locks the job and employee writes, then validates again against current data before creating anything. New conflicts refresh the draft's errors and create no employees. Successful creation, manager links, row result links and audit history commit together; any failure rolls them all back. A submission key makes repeated uploads return the same job, while a file hash prevents reusing that key for different content. Repeated/concurrent commits of the same reviewed version return the original result without duplicates. Existing employees are never updated. New records start active with no linked user; blank work schedule stays unassigned and blank event eligibility stays false. Management-office hours continue to use the existing configured schedule.
- Results and history: the completed job lists imported employee references, supports read-only staged-record inspection and provides a CSV results report of record numbers, field errors and created record IDs. Source personal field values are omitted from the report and spreadsheet formulas are escaped. Audit entries record action/version/counts and changed field names rather than copying source personal data into general activity logs. Cancellation closes a draft without deleting its records or importing them. Historical legacy jobs retain their prior totals but cannot run through the new commit path. Draft and completion timestamps are consistent even when the database session is not UTC.

Validation: the consolidated 271-test run passed 269 tests, including all existing-module and migration tests. Two import checks failed because the test CSV builder omitted optional columns appearing only in later rows; after fixing the fixture, all 15 import tests passed. A browser acceptance check uploaded synthetic data with a missing name, showed the validation failure, saved a correction, imported two employees together, and displayed uploaded/corrected/committed history. The preview revealed a timestamp mismatch for a non-UTC database session; a final creation-timestamp fix and regression brought the import suite to 16 passing tests. All 272 current tests therefore have passing verification across the consolidated run and targeted reruns. Final TypeScript and server build passed; the unchanged frontend passed its production build and browser check. Main minified frontend bundle remains approximately 1.699 MB. The isolated preview server and tab were closed. No production records, notifications, paid services, push or deployment were changed.

Remaining scope: source data must be reviewed by HR; imported identity references are not externally verified. XLSX upload, bulk updates/merges of existing employees, account provisioning, assignment to event teams, source-document uploads, retained-draft cleanup policy and production-scale acceptance are separate work. Imported employee records feed the existing workforce features once their required team memberships and qualifications are configured.


## Batch 13 — permission boundaries and release readiness (18 September 2026)

- Normalized legacy authorization to match Express's case-insensitive routes and decoded identifiers. Invalid IDs and encoded separators fail closed; recruitment statistics are protected. Employee subresource checks cover event profiles as well as leave, balances, shifts and documents.
- Applied employee scope in SQL to event profile/assignment/roster/rating/recipient collections. Event-manager writes require a visible employee. Ratings use the signed-in active employee and reject self-review and invalid criterion scores; communication authors and recipient acknowledgements cannot be forged, and event creators cannot be overwritten. This preserves the existing role matrix: legacy event-manager access covers temporary employees, while Workforce uses separate team grants.
- Removed duplicate recruitment/onboarding handlers and rejected unversioned application, candidate, individual checklist-task and direct leave-approval writes. Existing read aliases remain. Onboarding notes/cancellation use a current revision, mandatory reason and transactional private history. Employee/template/date/progress changes are excluded from this endpoint; task/document workflows control completion and cancellation blocks task changes.
- Added shared navigation/direct-page guards and an access-restricted screen. Administrator-only and organization-wide management pages no longer render forms for restricted roles. Self-service pages retain their existing API scopes. API responses use no-store, unknown API paths return JSON 404, and server shutdown clears the reminder interval.
- Added a current [release-readiness report](RELEASE-READINESS.md) distinguishing local implementation, evidence and remaining business/integration/production acceptance. No new migration was needed for this batch.

Validation: one consolidated run passed all **285 tests across 23 files**, including 13 new tests against actual application route registration and the complete migration journal. TypeScript and both production builds passed. Isolated browser acceptance verified controlled onboarding note saves and temporary-employee direct-page denial. It found one stale history cache; the UI invalidation was fixed, then final TypeScript/frontend builds passed and the browser immediately displayed the new history on reopen without a reload. The main frontend chunk is 1,704.41 KB minified / 454.10 KB gzip and still triggers the existing bundle-size warning. Preview used synthetic accounts/in-memory data and was closed. All changes remain local, uncommitted and unpushed; no deployment, production records, external messages or paid resources changed.

Next engineering scope: consolidate legacy reporting/export/communication and event screens, add versioned candidate corrections, and improve loading/accessibility for larger workflows. Production private-file delivery, approved business calculations, retention policy, Resend setup and actual EOS interoperability remain acceptance/integration work; WhatsApp stays deferred. This focused review is not an exhaustive security assessment.

## Batch 14 — reporting, candidate corrections and shared operations (18 September 2026)

Implemented owner-private report snapshots and exact saved CSV export; explicit headcount, date-based turnover, saved approved leave bookings, derived document expiry and site-timezone workforce delivery definitions; versioned administrator reporting policy; candidate correction history and identity locks after the first offer; a shared Workforce entry for event/FEC operations with earlier event records; and route-level loading with a recovery screen.

Validation: 294 of 295 tests passed in the consolidated run. The one failing new test omitted a mandatory synthetic timesheet note; after fixing the fixture, all 10 affected report/candidate tests passed. The combined result covers 295 tests across 24 files. TypeScript and both production builds passed. Main bundle: 612.14 KB minified / 178.78 KB gzip, down from 1,704.41 KB / 454.10 KB. A size warning remains. Isolated browser acceptance verified saved report generation/reopening, CSV action, candidate correction/version/history, shared Workforce and archive navigation, and a 390 px report layout. No production records were created.

Release reconciliation: freshly fetched GitHub main is f0904f7c90012d4bd4c8a8df37224925db510ccb. Render is live at f69d23839ddc99d9cd8a59a1893f82046adcfec4. Main has a separate implementation and migration history from this development branch. Do not deploy this branch directly or replace the live migration journal. Preserve it as the tested local-module branch, and port this batch onto the live baseline with additive migrations and an upgrade check. Earlier local batches require separate compatibility reconciliation before release.
